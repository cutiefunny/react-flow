# React Flow - Environment Variables Example

# ============================================
# API Configuration
# ============================================

# Use Mock API for development testing
# Set to 'true' to use mock data without backend server
# Set to 'false' or remove to use real FastAPI backend
VITE_USE_MOCK_API=true

# FastAPI Backend URL (used when VITE_USE_MOCK_API is false)
# VITE_API_BASE_URL=http://localhost:8000

# ============================================
# Development Mode
# ============================================

# Enable debug logging
VITE_DEBUG=false

# ============================================
# Notes for Migration
# ============================================
# 1. During development, set VITE_USE_MOCK_API=true
# 2. This uses in-memory mock API defined in mockApi.js
# 3. Mock data can be found in mockData.js
# 4. To switch to real backend:
#    - Set VITE_USE_MOCK_API=false
#    - Ensure your FastAPI backend is running
#    - Update VITE_API_BASE_URL if needed
# 5. The Mock API has the exact same interface as FastAPI
#    so switching is seamless!
